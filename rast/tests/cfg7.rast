#test success

type A[x] = +{ n : A[A[x]], l : +{l : x} }
type B[y] = +{ n : B[B[y]], l : C[y] }
type C[y] = +{ l : y }

decl id : (d : A[1]) |- (c : B[1])
proc c <- id d = c <-> d

(*
A[1] == B[1]
if A[A[1]] == B[B[1]] and +{l : 1} == C[1]
*)